# Archived Asset Production Scripts

These scripts generated superseded M01 and transfer-ring source packages. They are retained only as implementation history and must not be run against the active asset tree without first updating every input/output path to the current structure.

The original source inputs are available in the verified external backup `efv260710-assets-backup-20260710-102216` beside the repository.

Use `node scripts/audit-assets.mjs` for current asset maintenance.
