# EFV 运行资产审计（2026-07-12）

## 结果

- 正式运行资产：100 个文件，共 136.94 MiB。
- 直接运行引用：99 个；`assets/README.md` 为目录说明，不计入引用。
- 未引用资产：0。
- 缺失引用、完全重复文件、目录放置违规：0。
- 已归档备用资产：17 个，共 23.23 MiB，位于 `dict/legacy-assets-20260712/`。

## 本轮发布边界

- `assets/` 只保留当前页面、游戏脚本、样式和地图配置实际使用的文件。
- `dict/` 保留旧精灵、旧任务物件、旧特效、开发预览和候选图；不进入阿里云运行包。
- `share/` 中的合作开发者素材和文档本轮暂不进入 GitHub 更新或阿里云运行包。
- `release/`、`tmp/`、本地数据库、日志和缓存不进入全量运行包。

## 角色与地图交互复核

- 阿宇、江寻、老登启用新版人形行走、猫形奔跑与变身收束帧，旧版精灵归档到 `dict/legacy-assets-20260712/`。
- M02、M03 的任务点均已配置静态交互物件；M03 调查台碰撞范围缩小，任务触发半径扩大。
- M01 任务物件降低尺寸、光晕与标签显眼度，并保留小地图引导图标。
- 角色精灵协议、地图配置解析、JavaScript 语法和运行资产引用审计均通过。

## P1 结章视频

- 运行文件：`assets/cg/p1boss-end.mp4`，当前约 4.47 MiB。
- 登录阶段只加载视频元数据，不阻塞登录和 M1 地图。
- 进入 M3 后，在浏览器空闲时开始完整预载；省流模式或 2G 网络跳过此步骤。
- 进入 M4 后确保开始预载，地图关键资源优先于视频。
- 点击 M4 章节完成点后播放，播放结束或主动跳过后显示章节结算。
- 本地服务已支持 HTTP Range；阿里云/CDN 同样需要保留 `Accept-Ranges: bytes` 和 `video/mp4` 类型。
- 后续替换高清版时，应同步更新视频 URL 的版本参数，避免 CDN 命中旧缓存。
